# Skill Call Graph

Generated by `library-skill` on 2026-04-10.

Visual map of how all 46 skills in agent-loom call each other. Arrows show direction of invocation (`caller → callee`). Skills with no outgoing arrows are leaf nodes.

```mermaid
flowchart LR
  subgraph Meta
    usc[universal-skill-creator]
    imp[improve-skills]
    val[validate-skills]
    res[research-skill]
    pru[prune-skill]
    com[compress-skill]
    spl[split-skill]
    dep[deprecate-skill]
    lib[library-skill]
    pub[publish-skill]
    gen[generate-changelog]
    lfp[learn-from-paper]
    app[apply-paper-to-project]
    sec[secure-skill]
    sec-cs[secure-skill-content-sanitization]
    sec-ri[secure-skill-repo-ingestion]
    sec-rt[secure-skill-runtime]
  end

  subgraph Thinking
    bra[brainstorming]
    inv[inversion]
    adv[adversarial-hat]
    pre[pre-mortem]
    asm[assumption-mapping]
    soc[socratic]
    dt[deep-thinking]
    fp[first-principles]
    so[second-order]
    fer[fermi]
    ooda[ooda]
  end

  subgraph Project-Specific
    ps[product-soul]
    prd[prd-writing]
    ipl[implementation-plan]
    tdd[test-driven-development]
    asa[agent-system-architecture]
    adl[architectural-decision-log]
    tda[technical-debt-audit]
    cbu[codebase-understanding]
    crsp[code-review-crsp]
    daf[debug-and-fix]
    dd[reality-check]
    po[project-orchestrator]
    psu[project-setup]
  end

  subgraph Agent-Process-Design
    pd[process-decomposer]
    aa[agent-builder]
    se[setup-evaluation]
    sf[skill-finder]
    tf[tool-finder]
    cap[create-agent-prompt]
  end

  %% --- Meta skill call chains ---
  usc --> res
  usc --> spl
  usc --> com
  usc --> val
  usc --> lib
  usc --> pub

  imp --> val
  imp --> dep
  imp --> pru
  imp --> res
  imp --> spl
  imp --> com
  imp --> lib

  com --> spl
  spl --> lib
  dep --> lib
  lib --> gen

  lfp --> sec
  lfp --> usc
  lfp --> app
  lfp --> val

  app --> adl

  sf --> usc
  sf --> lib

  %% --- Security chain ---
  sec --> sec-cs
  sec --> sec-ri
  sec --> sec-rt

  %% --- Thinking skill chains ---
  dt --> inv
  dt --> pre
  dt --> asm
  dt --> soc
  dt --> adv
  dt --> fp
  dt --> so
  dt --> fer
  dt --> ooda

  bra --> inv
  prd --> inv
  ps --> inv

  %% --- Process & agent design chains ---
  pd --> sf
  pd --> tf
  pd --> res

  aa --> asa
  aa --> cap
  aa --> se
  aa --> po

  %% --- Due diligence ---
  dd --> adv
  dd --> asm
  dd --> cbu
  dd --> ipl

  %% --- Orchestrator ---
  po --> psu
```

## Reading the Graph

- **Entry points** — skills users invoke directly: `universal-skill-creator`, `improve-skills`, `learn-from-paper`, `due-diligence`, `project-orchestrator`, `project-setup`, `process-decomposer`, `agent-builder`, `deep-thinking`, `brainstorming`, `prd-writing`, `product-soul`
- **Meta chain** — `improve-skills` runs the full cycle: validate → deprecate → prune → research → rewrite → split/compress → library-skill → generate-changelog
- **Security gate** — `secure-skill` orchestrates three sibling skills (`content-sanitization`, `repo-ingestion`, `runtime`) and is called by `learn-from-paper` and internally by `research-skill`, `universal-skill-creator`, `improve-skills`
- **Process layer** — `process-decomposer` → `agent-builder` → `setup-evaluation` → `project-orchestrator` → execution
- **Leaf nodes** (called but call nothing): `validate-skills`, `research-skill`, `prune-skill`, `publish-skill`, `generate-changelog`, `tool-finder`, `create-agent-prompt`, `setup-evaluation`, and all thinking framework skills except `deep-thinking`
